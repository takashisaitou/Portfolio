<h1>社員投稿一覧</h1>
<%= render 'posts/post' %>

<h1>社員一覧</h1>
<% @users.each do |user| %>
<P><%= image_for(user) %></P>
<p>名前：<%= user.username %></p>
<p>所属部門：<%= user.department %></p>
<p>Eメール：<%= user.email %></p>
<% end %>

<%= render 'places/place' %>